
package rs.ac.bg.etf.pp1;

%%

%{

	private Symbol new_simbol(int type) {
		return new Symbol(type, yyline+1, yycolumn);
	}
	
	private Symbol new_symbol(int type, Object value) {
		return new Symbol(type, yyline+1, yycolumn, value);
	}

%}

%cup
%line
%column


%%

sekcija regex-a